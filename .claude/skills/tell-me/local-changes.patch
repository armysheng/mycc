diff --git a/.claude/skills/tell-me/SKILL.md b/.claude/skills/tell-me/SKILL.md
index 3a87544..ed9c18c 100644
--- a/.claude/skills/tell-me/SKILL.md
+++ b/.claude/skills/tell-me/SKILL.md
@@ -31,12 +31,27 @@ node /Users/aster/AIproject/mylife/.claude/skills/tell-me/send.js "标题" "内
 
 **参数说明**：
 - 标题：卡片标题
-- 内容：支持 lark_md 格式，用 `\n` 换行，`**粗体**`、`*斜体*`
+- 内容：支持富文本格式（每行一个元素）
 - 颜色（可选）：blue（默认）、green、orange、red
 
+**富文本格式**：
+| 语法 | 说明 |
+|------|------|
+| `# 标题` | 一级标题（加粗大字） |
+| `## 副标题` | 二级标题 |
+| `- 列表项` | 列表 |
+| `> 引用` | 引用块 |
+| `[文字](url)` | 链接 |
+| `img:图片URL` | 图片 |
+| `btn:按钮文字|跳转链接` | 按钮 |
+| `**粗体**` | 粗体 |
+| `*斜体*` | 斜体 |
+| `` `代码` `` | 行内代码 |
+| `空行` | 分割线 |
+
 **示例**：
 ```bash
-node /Users/aster/AIproject/mylife/.claude/skills/tell-me/send.js "任务完成" "• 完成了 xxx\n• 下一步 yyy" green
+node .claude/skills/tell-me/send.js "任务完成" "# 完成清单\n- 修复了 xxx bug\n- 新增了 yyy 功能\n\n## 下一步\nbtn:查看PR|https://github.com/..." green
 ```
 
 ## 卡片颜色
diff --git a/.claude/skills/tell-me/send.js b/.claude/skills/tell-me/send.js
index e563a21..9e65609 100644
--- a/.claude/skills/tell-me/send.js
+++ b/.claude/skills/tell-me/send.js
@@ -5,6 +5,13 @@
  * 颜色: blue(默认), green, orange, red
  */
 
+import { fileURLToPath } from 'url';
+import { dirname, join } from 'path';
+import { readFileSync } from 'fs';
+
+const __filename = fileURLToPath(import.meta.url);
+const __dirname = dirname(__filename);
+
 const [,, title, content, color = 'blue'] = process.argv;
 
 if (!title || !content) {
@@ -12,22 +19,111 @@ if (!title || !content) {
   process.exit(1);
 }
 
-// ⚠️ 请替换成你自己的飞书 webhook
-// 参考 配置SOP.md 获取 webhook 地址
-const webhook = 'YOUR_FEISHU_WEBHOOK_HERE';
-
-if (webhook === 'YOUR_FEISHU_WEBHOOK_HERE') {
+// 从 config.json 读取 webhook
+let webhook;
+try {
+  const config = JSON.parse(readFileSync(join(__dirname, 'config.json'), 'utf8'));
+  webhook = config.webhook;
+} catch (err) {
   console.error('❌ 飞书 webhook 未配置');
   console.error('');
   console.error('请按以下步骤配置：');
   console.error('1. 打开飞书客户端 → 创建群 → 设置 → 群机器人 → 添加自定义机器人');
   console.error('2. 复制 Webhook 地址');
-  console.error('3. 编辑 .claude/skills/tell-me/send.js，替换第 9 行的 webhook');
+  console.error('3. 创建 .claude/skills/tell-me/config.json：');
+  console.error('   {"webhook": "你的webhook地址"}');
   console.error('');
   console.error('详见：.claude/skills/tell-me/配置SOP.md');
   process.exit(1);
 }
 
+// 处理转义字符（Windows 命令行兼容）
+const unescape = (text) => {
+  return text
+    .replace(/\\n/g, '\n')
+    .replace(/\\t/g, '\t')
+    .replace(/\\"/g, '"');
+};
+
+// 解析内容中的特殊标记
+const parseContent = (text) => {
+  const elements = [];
+  const lines = unescape(text).split('\n');
+
+  for (const line of lines) {
+    if (!line) {
+      // 空行
+      elements.push({ tag: 'hr' });
+    } else if (line.startsWith('# ')) {
+      // 一级标题
+      elements.push({
+        tag: 'div',
+        text: { tag: 'lark_md', content: `**${line.slice(2)}**` }
+      });
+    } else if (line.startsWith('## ')) {
+      // 二级标题
+      elements.push({
+        tag: 'div',
+        text: { tag: 'lark_md', content: `***${line.slice(3)}***` }
+      });
+    } else if (line.startsWith('- ')) {
+      // 列表项
+      elements.push({
+        tag: 'div',
+        text: { tag: 'lark_md', content: line }
+      });
+    } else if (line.startsWith('> ')) {
+      // 引用
+      elements.push({
+        tag: 'div',
+        text: { tag: 'lark_md', content: line }
+      });
+    } else if (line.match(/^\[.*\]\(.*\)$/)) {
+      // 链接
+      elements.push({
+        tag: 'div',
+        text: { tag: 'lark_md', content: line }
+      });
+    } else if (line.startsWith('img:')) {
+      // 图片格式：img:URL
+      const imgUrl = line.slice(4);
+      elements.push({
+        tag: 'img',
+        img_key: imgUrl,
+        alt: { tag: 'plain_text', content: '图片' }
+      });
+    } else if (line.includes('|') && line.startsWith('|')) {
+      // 表格（简单处理：保持 markdown 格式）
+      elements.push({
+        tag: 'div',
+        text: { tag: 'lark_md', content: line }
+      });
+    } else if (line.startsWith('btn:')) {
+      // 按钮格式：btn:文字|URL
+      const [, btnText, btnUrl] = line.match(/btn:(.+)\|(.+)/) || [];
+      if (btnText && btnUrl) {
+        elements.push({
+          tag: 'action',
+          actions: [{
+            tag: 'button',
+            text: { tag: 'plain_text', content: btnText },
+            type: 'default',
+            url: btnUrl
+          }]
+        });
+      }
+    } else {
+      // 普通文本，支持 **粗体** *斜体* `代码`
+      elements.push({
+        tag: 'div',
+        text: { tag: 'lark_md', content: line }
+      });
+    }
+  }
+
+  return elements;
+};
+
 const card = {
   msg_type: 'interactive',
   card: {
@@ -36,10 +132,8 @@ const card = {
       template: color
     },
     elements: [
-      {
-        tag: 'div',
-        text: { content, tag: 'lark_md' }
-      },
+      ...parseContent(content),
+      { tag: 'hr' },
       {
         tag: 'note',
         elements: [{ tag: 'plain_text', content: `⏰ ${new Date().toLocaleString('zh-CN')}` }]
diff --git "a/.claude/skills/tell-me/\351\205\215\347\275\256SOP.md" "b/.claude/skills/tell-me/\351\205\215\347\275\256SOP.md"
index 632f12c..dfa4db0 100644
--- "a/.claude/skills/tell-me/\351\205\215\347\275\256SOP.md"
+++ "b/.claude/skills/tell-me/\351\205\215\347\275\256SOP.md"
@@ -18,12 +18,14 @@
 
 收到用户的 Webhook URL 后：
 
-### 1. 修改脚本
+### 1. 创建配置文件
 
-打开 `.claude/skills/tell-me/send.js`，把第 9 行的 webhook 替换成用户提供的 URL：
+创建 `.claude/skills/tell-me/config.json`：
 
-```js
-const webhook = '用户提供的URL';
+```json
+{
+  "webhook": "用户提供的URL"
+}
 ```
 
 ### 2. 测试
@@ -38,6 +40,8 @@ node .claude/skills/tell-me/send.js "测试" "飞书通知配置成功" green
 
 配置完成，以后说 `/tell-me` 或 `通知我` 就能收到飞书通知了。
 
+**注意**：config.json 已加入 .gitignore，拉取新版本不会覆盖本地配置。
+
 ---
 
 ## 跨平台说明
